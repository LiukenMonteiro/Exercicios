const animal = {
    nome: 'Jade',
    idade: 10,
    comida: 'petisco',
}

console.log(Object.keys(animal));
console.log(Object.values(animal));
console.log(Object.entries(animal));

//para acessar chaves do Object = Object.keys
//para acessar valores do Object = Object.values
//para acessar chaves e valores = Object.entries ( retorna um array de array's)